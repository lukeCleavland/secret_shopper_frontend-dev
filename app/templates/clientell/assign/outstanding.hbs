<div class="row">
    <div class="col-sm-12">
        <div class="alert alert-info alert-dismissible" role="alert">
            <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
            <p>Here are the assignments that shoppers have opted into.  Click <span class="label label-primary">Send Survey</span> to send a survey.</p>
            <button class="btn btn-default" style="margin-top: 10px;" {{action 'showLearnMoreModal'}}>Learn More</button>
        </div>
        <div class="panel panel-default">
            <div class="panel-heading">
                <h3 class="panel-title">Outstanding Surveys</h3>
            </div>
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>Survey</th>
                    <th>Restaurant</th>
                    <th>Shopper</th>
                    <th>Created On</th>
                    <th>Send</th>
                </tr>
                </thead>
                <tbody>
  
                {{#each a in this.sortedContent}}
                {{#unless a.isDirty}}
    {{#unless a.isTestRestaurant}}    
                  {{#unless a.isOld}}    

                    <tr>
                          <td>{{a.survey.name}}</td>
                          <td>{{a.restaurant.name}}</td>
                          <td>{{a.shopper.email}}</td>
                          <td>{{a.formattedDate}}</td>
                          <td><button {{bind-attr class=":btn a.optedIn:btn-primary:btn-default a.optedIn::disabled"}} {{action 'sendSurvey' a}}>Send survey</button></td>
                      </tr>
                {{/unless}}
{{/unless}}
                {{/unless}}
                {{/each}}
                </tbody>

            </table>
        </div>
    </div>
</div>
<div id="learn-more-modal" class="modal fade" data-backdrop="static" data-keyboard="false" tabindex="-1" role="dialog" aria-labelledby="loading-label" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title" id="add-category-label">Help</h4>
            </div>
            <div class="modal-body">
                <p>Here's your outstanding surveys.</p>
                <p>On this page, all the people who 'opted in' to their survey assignment can be sent the actual survey.</p>
                <p>You can send a survey as many times as you'd like, just in case they lost the email and need another.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal" aria-hidden="true">OK</button>
            </div>
        </div>
    </div>
</div>
